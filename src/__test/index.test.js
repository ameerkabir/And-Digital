import {filterCandidateBySkill} from '../helper';
describe('Filter function', ()=>{
    let result;
    let err;
    beforeAll(()=>{
        result = null;
        err = null;
    })
    // it('filterCadidateBySkill should fail if no skill')
    it('should ')
})